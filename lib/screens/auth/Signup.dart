import 'package:chat/screens/auth/components/signupBody.dart';
import 'package:flutter/material.dart';

class Signupcreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SignupBody(),
    );
  }
}
